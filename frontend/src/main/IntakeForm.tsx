/**
 * @todo Write separate components to create the full intake form using styling from login (under start folder)
 * @todo Match up all necessary need information
 * @todo Create proper styling to be integrated into main app
 * @returns In take form structure
 */
export default function IntakeForm(){
    return <></>
}

